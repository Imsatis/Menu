#include<iostream>
#include<conio.h>

using namespace std;

    int main()

{
   int count,chk;

   string a,b,c,d;
     
     switch(chk)
     {  if(a.empty())
       { 
         getline(cin,a);
	 cin>>chk;
         case 0:
	 break;

          }
       else
       { 
        count++;
         }
        if(b.empty())
       { 
         getline(cin,b);
         cin>>chk;
	 case 1:
	 break;

	 }
       else
       { 
        count++;
         }
  
       if(c.empty())
       { 
         getline(cin,c);
         cin>>chk;
	 case 2:
	 break;

	 }
       else
       { 
        count++;
         }

        if(d.empty())
       { 
         getline(cin,d);
         cin>>chk;
	 case 3:
	 break;

	 }

       else
       { 
        count++;
         }
      }

       getch();




}    
