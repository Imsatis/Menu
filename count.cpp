#include<iostream>
#include<conio.h>

using namespace std;

    int main()

{
   int count,chk;

   string a,b,c,d;
     
     switch(chk)
     {  if(a==)
       { 
         getline(cin,a);
	 cin>>chk;
         case 0:
	 break;

          }
       else
       { 
        count++;
         }
        if(b==char)
       { 
         getline(cin,b);
         cin>>chk;
	 case 0:
	 break;

	 }
       else
       { 
        count++;
         }
  
       if(c==char)
       { 
         getline(cin,c);
         cin>>chk;
	 case 0:
	 break;

	 }
       else
       { 
        count++;
         }

        if(d==char)
       { 
         getline(cin,d);
         cin>>chk;
	 case 0:
	 break;

	 }

       else
       { 
        count++;
         }
      }

       getch();
}    
